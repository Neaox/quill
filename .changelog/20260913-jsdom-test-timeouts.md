Fixed: The UI and editor test projects give Testing Library's `findBy*` five seconds rather than its default one, as the web project already does. Mounting ProseMirror into jsdom and driving it with `userEvent` is slow enough that a full parallel run pushed a query past that one second and reported a slow render as a missing element. Vitest's own per-test timeout, not this, is what still bounds a test that has actually hung.
