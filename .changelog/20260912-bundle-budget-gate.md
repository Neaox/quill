Added: `pnpm check:bundle` (`scripts/check-bundle.ts`) measures each route's initial gzip/brotli bundle size against the budgets in `quill-plan.md` section 31 and prints a per-route table plus the five largest files in the reading route's graph. Not yet part of `pnpm check`: the reading route is currently over its 250 KB budget pending a reduction pass, so CI runs it as a non-blocking step for now (see `docs/operations/bundle-budget.md`).
