Added: `@quill/search` provides the engine-agnostic search core (ADR-010): a query language parser and serializer, the `IndexableDocument` projection from a published document's front matter and outline, `RankingProfile` data, an offset-based snippet builder, the mandatory `VisibilityFilter` contract, and a `createSearchService` composition, with an in-memory `SearchIndex` for testing it.
