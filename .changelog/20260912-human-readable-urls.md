Added: Every document now carries a short, permanent public key — ten Crockford base-32 characters, unique across the instance — so a link can read `/w/engineering/d/authentication-architecture-k7m3q9v2xd` instead of two UUIDs (ADR-035). Existing documents are given a key by the migration; the UUID form of every address keeps working.
Added: Every `/api/documents/:id` route accepts a document's short key, with or without the title words in front of it, as readily as its UUID, and every `/api/workspaces/:id` route accepts a workspace slug. Document responses and navigation tree nodes carry `shortId`, and a `slug` derived from the current title.
Added: `PATCH /api/workspaces/:id` can move a workspace's slug; the slug it leaves behind keeps redirecting to it for a year, so links written before the move still resolve.
Added: Publishing rewrites any link an author pasted out of the address bar into the canonical `/d/<uuid>` form, so backlinks and the broken-link signal see every internal link however it was written.
